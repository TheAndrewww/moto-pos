# Configuración Railway.
# Asume plugin Postgres agregado (DATABASE_URL inyectado).

[build]
builder = "nixpacks"

[deploy]
# Nixpacks copia el binario producido (nombre = package name) a ./bin
startCommand = "./bin"
restartPolicyType = "always"
